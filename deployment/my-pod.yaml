apiVersion: v1
kind: Pod
metadata:
  namespace: default
  name: my-pod
  annotations:
     k8s.v1.cni.cncf.io/networks: loopback-conf
spec:
  nodeSelector:
    kubernetes.io/hostname: ovn-worker
  containers:
  - name: my-pod
    image: quay.io/wizhao/ft-base-image:0.9
    command:
      - "/sbin/init"
    imagePullPolicy: IfNotPresent
